import React from 'react'
// khi đặt tên trong dấu ngoặc tròn thư mục này sẽ không được coi là 1 root
// Mục đích chỉ để nhóm các root liên quan tới nhau
// Vẫn có thể tạo các trang đặc biệt như layout, loading, error để dùng chung cho các root trong thư mục này
// lưu ý không để trùng tên các root
const SingInPage = () => {
  return (
    <div>SingInPage</div>
  )
}

export default SingInPage