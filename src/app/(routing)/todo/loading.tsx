import React from 'react'
// loading.tsx cũng là 1 trang đặc biệt nên cần viết đúng tên
// Mỗi root đều có thể có trang loading riêng để quản lý loading state(streaming:WEB hiển thị dần dần không phải chờ load xong hết mới thấy)
const loading = () => {
  return (
    <div>loading...</div>
  )
}

export default loading